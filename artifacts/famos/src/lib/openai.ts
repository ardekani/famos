/**
 * OpenAI client stub (server-side only — do not expose OPENAI_API_KEY to the browser).
 * This file is a placeholder. Actual OpenAI calls should be made through
 * the API server (artifacts/api-server), never directly from the frontend.
 *
 * In the API server, you can initialize the client like this:
 *
 * import OpenAI from "openai";
 * const openai = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });
 */

export const OPENAI_NOTE =
  "OpenAI calls are made server-side via the API server. See artifacts/api-server.";
